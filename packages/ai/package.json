{
  "name": "@sps/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Search and evaluation. Depends on the engine; runs in a Worker and in Node.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@sps/engine": "workspace:*"
  }
}
