{
  "name": "@sps/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Game rules. Pure: no DOM, no timers, no Math.random, no dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
