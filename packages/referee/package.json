{
  "name": "@sps/referee",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The server-authoritative room, as pure data. Holds no socket, no storage and no clock of its own: every operation is handed the connection, the message and `now`.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc --noEmit -p tsconfig.json" },
  "dependencies": {
    "@sps/engine": "workspace:*",
    "@sps/match": "workspace:*"
  }
}
