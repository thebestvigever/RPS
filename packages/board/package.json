{
  "name": "@sps/board",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Rendering: a pure function from position, theme, family and appearance to an SVG string. No DOM, no React — apps/web injects the markup. docs/VISUAL_SYSTEM.md is normative for what this draws.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc --noEmit -p tsconfig.json" },
  "dependencies": { "@sps/engine": "workspace:*" }
}
