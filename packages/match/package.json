{
  "name": "@sps/match",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Running a game: clocks, offers and time gifts. Pure — it holds no timers and asks the caller for the time.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "typecheck": "tsc --noEmit -p tsconfig.json" },
  "dependencies": { "@sps/engine": "workspace:*" }
}
